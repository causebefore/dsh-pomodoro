# dsh-pomodoro 代码评审问题清单

- 生成日期：2026-08-14
- 核查范围：两轮代码评审结论去重与校正
- 状态说明：“存在”表示当前代码仍需处理；“记录性偏差”表示已知契约差异但不阻塞运行；“已修复”表示当前工作区已不再存在；“建议保留/核对通过”表示无需修改。

| 编号 | 问题 | 结论 | 说明 | 证据链 | 建议修改方案 |
|---|---|---|---|---|---|
| POM-001 | 设置并发控制未接通 | **存在，中优先级** | `settingsTail` 只能串行单页面请求，不能防止多标签页或外部修改覆盖。客户端保存了 `revision`，写入时却未使用。 | [`lib/client.js:200`](lib/client.js#L200) → [`lib/client.js:223`](lib/client.js#L223) → [`lib/index.js:88`](lib/index.js#L88) → `C:\Users\lbq08\Desktop\deepseek-harness\docs\subsystems\settings.zh.md:128` | 草稿开始编辑时保存 `baseRevision`；RPC 携带 `expectedRevision`；服务端改用 `provider.update/replace`；捕获 `SETTINGS_CONFLICT` 并返回结构化冲突结果。 |
| POM-002 | 设置刷新会清空未保存草稿 | **存在，中优先级；与 POM-001 合并修复** | 任意快照发布都会执行 `setDrafts(null)`，包括外部修改、连接重置和加载错误。 | [`lib/client.js:185`](lib/client.js#L185) → [`lib/client.js:234`](lib/client.js#L234) → [`lib/client.js:400`](lib/client.js#L400) | 没有草稿时才自动采用新快照；存在草稿时保留草稿及其 `baseRevision`，显示外部更新提示；仅在本页面保存成功或用户明确重新加载时清空。 |
| POM-003 | 计时到期后先点击暂停可能漏切换 | **存在，中优先级** | 暂停直接写入 `endAt - Date.now()`；如果已经到期，会冻结负数。停止后 tick 不再完成阶段切换和番茄计数。 | [`lib/client.js:112`](lib/client.js#L112) → [`lib/client.js:131`](lib/client.js#L131) | 提取统一的 `settleExpired(now)`，由 `tick()` 和 `toggleRun()` 共同调用；先结算到期阶段，再执行暂停，并保证 `remainingMs >= 0`。 |
| POM-004 | `describe()` 未执行秘密字段脱敏 | **存在，发布前修复** | 当前没有秘密字段，因此没有即时泄漏，但违反所有 wire surface 必须脱敏的契约。 | [`lib/index.js:56`](lib/index.js#L56) → `C:\Users\lbq08\Desktop\deepseek-harness\docs\subsystems\settings.zh.md:98` | 改为 `binding.provider.describe({ redactSecrets: true })`。 |
| POM-005 | 客户端 `inject` 过度声明 | **存在，低至中优先级** | 声明四个硬依赖导致缺席防护不可达；但只保留 `timer` 又会让 `slots` 的激活时序不可靠。 | [`lib/client.js:177`](lib/client.js#L177) → [`lib/client.js:559`](lib/client.js#L559) | 推荐改为 `exports.inject = ["timer", "slots"]`；`connection`、`remote` 继续使用 `ctx.get()` 并保留降级处理。 |
| POM-006 | `z-index: 1000` 越过宿主层级管理 | **存在，低优先级** | 父级 stacking context 限制其全局层级，但它仍会改变 overlay 内部项目顺序，绕过 slot 排序。 | [`lib/client.js:15`](lib/client.js#L15) → `C:\Users\lbq08\Desktop\deepseek-harness\packages\client\ui-layout\src\client\AppFrame.module.css:110` | 删除 `.pomo-panel` 的 `z-index: 1000`。 |
| POM-007 | `.pomo-panel` 的 `pointer-events: auto` 重复 | **存在，但只是冗余** | 宿主已经对 overlay 直接子元素设置 `pointer-events: auto`。 | [`lib/client.js:15`](lib/client.js#L15) → `C:\Users\lbq08\Desktop\deepseek-harness\packages\client\ui-layout\src\client\AppFrame.module.css:116` | 与 POM-006 一起删除。 |
| POM-008 | 窄侧栏按钮缺少明确无障碍名称 | **存在，低优先级** | 按钮有 `title` 和 emoji，但实际读法依赖浏览器及辅助技术，不够稳定。 | [`lib/client.js:377`](lib/client.js#L377) → [`lib/client.js:389`](lib/client.js#L389) | 给按钮增加 `aria-label="番茄钟"`，给 emoji span 增加 `aria-hidden="true"`。 |
| POM-009 | 保存成功提示与实际生效时机不一致 | **存在，低优先级** | 空闲且当前阶段尚未消耗时，新时长立即生效；提示却始终称下次阶段切换时生效。 | [`lib/client.js:169`](lib/client.js#L169) → [`lib/client.js:454`](lib/client.js#L454) | 将提示简化为“已保存”。 |
| POM-010 | 全局 `<style>` 而非 CSS Modules | **存在，记录性偏差** | client-modules 可以管理 `style[data-plugin]`，证明 HMR 机制安全，但不等于官方样式规范正式豁免。 | [`lib/client.js:1`](lib/client.js#L1) → `C:\Users\lbq08\Desktop\deepseek-harness\docs\web-styling.zh.md:11` → `C:\Users\lbq08\Desktop\deepseek-harness\packages\client\modules\src\client\system.ts:37` | 当前不引入构建链；在 README 记录无构建 bundle 的项目级例外。引入构建步骤后再迁移 CSS Modules。 |
| POM-011 | UI 与 slot label 硬编码中文 | **存在，产品范围观察项** | 英文 locale 下仍显示中文；对明确只面向中文用户的插件不构成功能错误。 | [`lib/client.js:542`](lib/client.js#L542) → [`lib/client.js:547`](lib/client.js#L547) | 先在 README 明确中文范围；确定需要多语言后再接入 locale 服务。 |
| POM-012 | 默认值存在多处副本 | **存在，低风险一致性问题** | 生产重复是 Config schema 与客户端启动兜底；`debug.html` 的 `fakeBase` 属测试 fixture。 | [`lib/index.js`](lib/index.js) → [`lib/client.js:81`](lib/client.js#L81) → [`debug.html`](debug.html) | 保持 schema 为权威来源；加强客户端同步注释，最好增加静态测试核对默认值。 |
| POM-013 | 保存时把四个字段全部写入 user 层 | **存在，但属于 UX 语义选择** | 即使用户只修改一个字段，保存后四个字段都会成为显式用户覆盖；解析结果正确，但会削弱继承层语义。 | [`lib/client.js:447`](lib/client.js#L447) → [`lib/index.js:92`](lib/index.js#L92) | 暂时可保留；以后若强调逐字段继承，再记录 changed fields，并使用 `provider.mutate(..., expectedRevision)`。 |
| POM-014 | 阶段完成时连续两次状态发布 | **存在，但性能影响较低** | 确实产生两次 store emit，但 React 18 通常会把同一任务中的更新批处理为一次渲染。 | [`lib/client.js:120`](lib/client.js#L120) | 不阻塞发布；修改 POM-003 的状态机时，可顺手合并为一次 `set()`。 |
| POM-015 | 两处 settings 降级提示文案近似 | **存在，但不建议单独抽象** | 两条提示面对的实际场景略有不同，抽成常量节省代码有限。 | [`lib/client.js:408`](lib/client.js#L408) → [`lib/client.js:420`](lib/client.js#L420) | 保留现状，除非后续统一设置错误状态组件。 |
| POM-016 | 旧 `config` 子端点与毫秒解析无调用方 | **已修复** | 无调用方的 `config` 子端点、客户端生产毫秒解析和 debug 模拟分支已经删除；自定义 `/pomodoro` settings RPC 仍然必要。 | [`lib/index.js:84`](lib/index.js#L84) → [`lib/client.js:149`](lib/client.js#L149) | 无需继续删除 `/pomodoro` RPC。 |
| POM-017 | `reset()` 无效写入 `endAt` | **已修复** | 当前重置只恢复剩余时间、总时长和运行状态。 | [`lib/client.js:139`](lib/client.js#L139) | 无需修改。 |
| POM-018 | 数字输入使用 `parseInt` | **已修复** | 当前已改为 `Number()` 和 `Number.isSafeInteger()`。 | [`lib/client.js:434`](lib/client.js#L434) | 无需修改。 |
| POM-019 | `Config(config)` 二次解析 | **有意防御，建议保留** | 保证入口被直接调用时仍共享 schema 默认值，幂等且成本很低。 | [`lib/index.js:40`](lib/index.js#L40) | 保留现状及契约注释。 |
| POM-020 | bundle、manifest、slot、RPC、token、peer 等核心骨架 | **核对通过** | 两轮审查均未发现结构性契约问题，主要风险集中在并发写入和计时状态机。 | [`package.json`](package.json)、[`cordis.patch.yml`](cordis.patch.yml)、[`lib/index.js`](lib/index.js)、[`lib/client.js`](lib/client.js) | 保持现有架构，无需重构 RPC 或 slot 方案。 |
