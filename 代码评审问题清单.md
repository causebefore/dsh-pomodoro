# dsh-pomodoro 代码评审问题清单

- 生成日期：2026-08-14
- 核查范围：两轮代码评审结论去重、rc.6 已安装包交叉验证与修复结果
- 证据边界：公开契约以 `docs/user/develop/` 为准；原评审引用的 `docs/subsystems/`、`web-styling.zh.md` 只作为历史审查输入，不再单独认定为公开插件教程要求。
- 状态说明：“已修复”表示当前工作区已消除问题；“已处理”表示已作出明确取舍并补充契约；“观察项/有意保留”表示当前无需修改。

| 编号 | 问题 | 结论 | 说明 | 证据链 | 建议修改方案 |
|---|---|---|---|---|---|
| POM-001 | 设置并发控制未接通 | **已修复，中优先级** | 客户端以草稿开始时的 revision 作为 `expectedRevision`；服务端在 provider 写队列内校验，陈旧写入返回结构化 `SETTINGS_CONFLICT`，不会覆盖新值。 | [`lib/client.js:238`](lib/client.js#L238) → [`lib/client.js:261`](lib/client.js#L261) → [`lib/index.js:99`](lib/index.js#L99) → [`lib/index.js:116`](lib/index.js#L116) | 已按 `{ value, expectedRevision }` / `{ expectedRevision }` 接通保存与重置；冲突后读取最新快照并保留草稿。 |
| POM-002 | 设置刷新会清空未保存草稿 | **已修复，中优先级** | 订阅回调现在只更新 snapshot；草稿持有 `baseRevision`，外部更新派生冲突提示。仅保存、清除或明确重新加载成功后清空草稿。 | [`lib/client.js:426`](lib/client.js#L426) → [`lib/client.js:452`](lib/client.js#L452) → [`lib/client.js:455`](lib/client.js#L455) → [`lib/client.js:481`](lib/client.js#L481) | 已保留草稿并禁用陈旧保存/清除，提供“重新加载”入口。 |
| POM-003 | 计时到期后先点击暂停可能漏切换 | **已修复，中优先级** | `tick()` 与暂停入口共享 `settleExpired(now, pauseNext)`；到期时先完成计数和阶段切换，再把新阶段停在完整时长，不再冻结负数。 | [`lib/client.js:114`](lib/client.js#L114) → [`lib/client.js:137`](lib/client.js#L137) → [`lib/client.js:148`](lib/client.js#L148) | 已统一到期结算；自然到期仍遵循 auto-start，显式暂停优先停止新阶段。 |
| POM-004 | `describe()` 未执行秘密字段脱敏 | **已修复，契约风险** | 浏览器 wire surface 已显式请求脱敏；当前数字和布尔 schema 行为不变。 | [`lib/index.js:56`](lib/index.js#L56) | 已改为 `describe({ redactSecrets: true })`。 |
| POM-005 | 客户端 `inject` 与缺席防护不一致 | **已处理，保留四项硬依赖** | rc.6 的 fiber 会等待全部 inject 服务激活；若把 `connection` / `remote` 改为一次性可选 `ctx.get()`，服务晚到时不会自动重建设置 scope。原缺席分支因此确属不可达。 | [`lib/client.js:80`](lib/client.js#L80) → [`lib/client.js:193`](lib/client.js#L193) → [`lib/client.js:266`](lib/client.js#L266) → [`lib/client.js:627`](lib/client.js#L627)；`docs/user/develop/framework/service.zh.md:21-32,102-109` | 选择保留 `timer/slots/connection/remote` 四项生命周期硬依赖，删除 `slots === undefined`、`connection !== undefined` 和 nullable scope 防御，并补充注释。 |
| POM-006 | `z-index: 1000` 越过宿主层级管理 | **已修复，低优先级** | 面板由 `shell.overlay` 宿主层承载，无需再在插件内抬高层级；当前 `.pomo-panel` 已无 z-index。 | [`lib/client.js:15`](lib/client.js#L15) → `C:\Users\lbq08\.dsh\profiles\node_modules\@deepseek-ai\dsh-client-ui-layout\lib\client.js:56`（rc.6） | 已删除 `z-index: 1000`。 |
| POM-007 | `.pomo-panel` 的 `pointer-events: auto` 重复 | **已修复，纯冗余** | rc.6 overlay 容器统一禁用命中并为直接子元素恢复 `pointer-events: auto`；插件重复声明已删除。 | [`lib/client.js:15`](lib/client.js#L15) → `C:\Users\lbq08\.dsh\profiles\node_modules\@deepseek-ai\dsh-client-ui-layout\lib\client.js:56`（rc.6） | 已与 POM-006 一起删除，最终需在真实宿主中确认点击与叠放。 |
| POM-008 | 窄侧栏按钮缺少明确无障碍名称 | **已修复，低优先级** | 宽、窄形态按钮现在都有稳定的 `aria-label="番茄钟"`；emoji 从无障碍树隐藏。 | [`lib/client.js:414`](lib/client.js#L414) → [`lib/client.js:419`](lib/client.js#L419) | 已补 `aria-label` 与 `aria-hidden`。 |
| POM-009 | 保存成功提示与实际生效时机不一致 | **已修复，低优先级** | 成功提示不再承诺固定的生效时机，兼容空闲立即生效和运行中下阶段生效两种路径。 | [`lib/client.js:510`](lib/client.js#L510) | 已简化为“已保存”。 |
| POM-010 | bundle 内注入 `<style>` 而非 CSS Modules | **已处理，项目实现选择** | 当前公开 `docs/user/develop/` 未把 CSS Modules 规定为第三方插件契约；本项目又无构建步骤，直接发布 `lib/client.js`。样式标签带插件归属信息并有防重复守卫。 | [`lib/client.js:13`](lib/client.js#L13) → [`lib/client.js:61`](lib/client.js#L61) → [`README.md:77`](README.md#L77) | 现状不增加构建链；已在 README 记录实现边界。以后引入构建步骤时再评估 CSS Modules。 |
| POM-011 | UI 与 slot label 硬编码中文 | **观察项，已记录产品范围** | 英文 locale 下仍显示中文；当前插件定位为简体中文界面，不构成功能错误。 | [`lib/client.js:609`](lib/client.js#L609) → [`lib/client.js:615`](lib/client.js#L615) → [`README.md:64`](README.md#L64) | 已在 README 明示；确定有多语言需求后再接入 locale 服务并重新注册 label。 |
| POM-012 | 默认值存在多处副本 | **已处理，保留必要副本** | schema 是生产权威来源；浏览器 bundle 在 RPC 首次返回前必须自行提供启动值，debug 的 `fakeBase` 是测试 fixture。 | [`lib/index.js`](lib/index.js) → [`lib/client.js:70`](lib/client.js#L70) → [`debug.html:199`](debug.html#L199) | 保留必要副本，并已增加“与 Config schema 默认值同步”的契约注释。 |
| POM-013 | 保存时把四个字段全部写入 user 层 | **有意保留，UX 语义选择** | 设置页是完整表单，保存即形成四字段快照；解析结果与并发语义正确，但会让四字段都成为显式覆盖。 | [`lib/client.js:502`](lib/client.js#L502) → [`lib/index.js:116`](lib/index.js#L116) | 当前保留，避免为非目标需求引入 changed-fields 状态；若以后强调逐字段继承，再改用 `mutate(..., expectedRevision)`。 |
| POM-014 | 阶段完成时连续两次状态发布 | **已修复，随 POM-003 合并** | `settleExpired()` 在一次 `set()` 中同时更新阶段、时长、运行态、计数与闪烁时间。 | [`lib/client.js:114`](lib/client.js#L114) | 无需额外修改。 |
| POM-015 | 两处 settings 降级提示文案近似 | **已处理，重复分支已删除** | nullable settings scope 是四项硬依赖下的不可达状态，删除后保留的提示分别对应远程不可写、加载失败、加载中和 provider 不可写，语义不同。 | [`lib/client.js:434`](lib/client.js#L434) → [`lib/client.js:444`](lib/client.js#L444) | 不再抽常量；保留四个实际可达状态的独立文案。 |
| POM-016 | 旧 `config` 子端点与毫秒解析无调用方 | **已修复** | `/pomodoro` 只保留 `settings.read/save/reset`；生产解析只接受 descriptor 的分钟形态。debug 也已同步真实协议。 | [`lib/index.js:107`](lib/index.js#L107) → [`lib/index.js:130`](lib/index.js#L130) → [`lib/client.js:167`](lib/client.js#L167) | 无需继续删除自定义 `/pomodoro` settings RPC。 |
| POM-017 | `reset()` 无效写入 `endAt` | **已修复** | 重置只恢复剩余时间、总时长和运行状态，下一次开始会重新计算 `endAt`。 | [`lib/client.js:155`](lib/client.js#L155) | 无需修改。 |
| POM-018 | 数字输入使用 `parseInt` | **已修复** | 当前使用 `Number()` 和 `Number.isSafeInteger()`，科学计数法按 number input 的真实数值解析。 | [`lib/client.js:468`](lib/client.js#L468) | 无需修改。 |
| POM-019 | `Config(config)` 二次解析 | **有意防御，建议保留** | 保证入口被直接调用时仍共享 schema 默认值，幂等且成本很低。 | [`lib/index.js:40`](lib/index.js#L40) | 保留现状及契约注释。 |
| POM-020 | bundle、manifest、slot、RPC、token、peer 等核心骨架 | **核对通过** | 两轮审查及 rc.6 已安装包交叉验证均未发现结构性问题；主要风险已在 POM-001 至 POM-004 中修复。 | [`package.json`](package.json)、[`cordis.patch.yml`](cordis.patch.yml)、[`lib/index.js`](lib/index.js)、[`lib/client.js`](lib/client.js) | 保持现有架构，无需重构 RPC 或 slot 方案。 |
